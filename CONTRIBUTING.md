# Contributing

Thanks for helping improve `action-approval-skill`.

Before opening a pull request:

1. Keep fixtures deterministic and free of live secrets.
2. Add or update tests for behavior changes.
3. Run `npm run release:check`.

Release-candidate changes should keep the CLI local-first and avoid adding network calls or live connector side effects.
